# Frozen EFGM v0.2 Experimental Baseline

The immutable comparison baseline for the EFGM experimental research lineage is:

```text
commit: b717f611a0d09bd8e52bc1b0be5ee178eecacf25
branch: baseline/efgm-v0.2-frozen
```

The baseline branch is a reference pointer only and must not be advanced. Research, benchmark, runner, configuration, and candidate-model work must branch from this commit or record this commit explicitly as the comparison baseline.

A later accepted model version does not retroactively change this baseline. Experiments must record the exact baseline SHA they compare against.
