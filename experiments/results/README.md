# Experiment Results

Store completed experiment summaries here. Each result should reference its manifest, repository/code SHA, scoring configuration ID, dataset version, scorers, baselines, counterexamples, regressions, and limitations.

Do not overwrite unfavorable prior results when a later candidate improves. Preserve the research trail.
