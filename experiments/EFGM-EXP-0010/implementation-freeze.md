# EFGM-EXP-0010 implementation freeze

Implementation freeze commit lineage culminates in branch head `d10b0cc92bfd008a9380cb04d0047f523a08c291` immediately before this declaration file.

The frozen scientific implementation consists of:

- `experiments/EFGM-EXP-0010/preregistration.md`
- `src/efgm/exp0010_trajectory_coherence.py`
- `tests/test_exp0010_trajectory_coherence.py`
- `.github/workflows/efgm-check.yml` EXP-0010 execution step

No scientific coefficient, checkpoint, factor, label rule, comparator, success criterion, or original EFGM notation may be altered after the first valid scientific execution to rescue the result.

This declaration does not itself change the experiment implementation. The first valid CI execution at or after this freeze declaration is authoritative, with exact execution identity recorded after completion.
