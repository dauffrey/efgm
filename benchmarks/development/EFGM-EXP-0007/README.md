# EFGM-EXP-0007 Development Cases

These 16 synthetic development specifications test canonical EFGM v2 sparse-failure diagnostics while preserving frozen `DQ`.

They include:

- four complete positive-prerequisite collapses on the narrow candidate path set;
- five single-metric extreme output/behavioral/operational entropy failures;
- one deliberately low non-critical grounding control;
- three positive borderline controls;
- three moderate single-penalty controls.

All labels are internally authored and EFGM-aware. This is tuning-visible development material, not holdout evidence.
