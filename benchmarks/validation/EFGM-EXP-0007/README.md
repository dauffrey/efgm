# EFGM-EXP-0007 Validation Cases

These 20 synthetic validation specifications remain internally authored but are separated from the development variants.

They include:

- four narrow-candidate prerequisite paths at `0.35`;
- four catastrophic positive paths outside the narrow candidate set (`reasoning_continuity`, `semantic_coherence`, `traceability`, `factual_consistency`);
- four sparse extreme penalty cases on different output/behavioral/operational metrics;
- one low non-critical grounding control;
- four positive borderline controls;
- three moderate penalty controls.

The four uncovered positive failures are preregistered before execution specifically to test semantic path-set completeness. This split is not sealed holdout evidence.
